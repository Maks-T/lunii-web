import React from 'react';
import { AppButton } from '@/components/shared/ui/AppButton';
import {CatalogBanner} from "@/features/catalog/components/CatalogBanner";

export const CatalogHero = () => {
    return (
      <CatalogBanner />
    );
};
